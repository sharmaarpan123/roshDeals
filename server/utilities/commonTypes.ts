export enum ROLE_TYPE_ENUM {
    ADMIN = 'admin',
    USER = 'user',
}

export enum POSTER_ENUM {
    REDIRECT = 'redirect',
    BRAND = 'brand',
    DEALCATEGORY = 'dealCategory',
    DEAL = 'deal',
}
