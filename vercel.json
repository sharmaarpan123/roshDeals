{
    "version": 2,
    "builds": [
        {
            "src": "dist/server/server.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/dist/server/(.*)",
            "dest": "server.js"
        }
    ]
}
